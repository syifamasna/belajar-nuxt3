export interface Products {
    id?: number,
    name?: string,
    category?: string,
    price?: string,
    image?: string,
    color?: string,
    isCart?: boolean,
    description?: string,
    quantity?: number,
    }